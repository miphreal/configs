# Intro

A bundle of user configs

There are conky, shell, vim.

# How to install:

    $ git clone git@github.com:miphreal/configs.git
    $ cd configs
    $ . install-shell
    $ . install-vim
    $ . install-conky